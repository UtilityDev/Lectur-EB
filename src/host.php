<?php
require("connection.php");
$roomCode = $_GET["roomCode"];

echo $roomCode;

// $SQL = "INSERT INTO Rooms VALUES ($roomCode)";
// $conn -> query($SQL);

// Use JQuery + unload to delete the element from sql table